'use strict';

/**
 * The function converts minutes into a seconds
 *
 *
 * @param {number} minutes - some number.
 * @returns {number} returns the number of seconds.
 */

export const convert = (minutes) => minutes * 60;
